#include "menu.h"

int menu()
{
    /*
	t_menu m;

	m_init(g, &m);

	while (g->g_state != EXIT)
	{
		g->g_state = m_events(g);
		//m_update(&m, g);
		m_render(g, &m);
		if (g->g_state == PLAY)
		{
			//m_free(&m);
			//play(g);
			//m_init(&m);
		}
		SDL_Delay(1);
	}
	//m_free(m);
	return (1);
    */
}